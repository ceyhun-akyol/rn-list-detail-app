export type CartItemType = {
  name: string;
  price: string;
  id: string;
  count: number;
};

export type ItemType = {
  createdAt: string;
  name: string;
  image: string;
  price: string;
  description: string;
  model: string;
  brand: string;
  id: string;
};
